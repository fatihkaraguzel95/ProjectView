# Supabase — copy to .env and fill in from Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
# Use the anon / publishable key (safe for the browser). NEVER the service_role key.
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx
